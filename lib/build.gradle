buildscript {
    ext.spigot_version = "1.16.3-R0.1-SNAPSHOT"
}

plugins {
    id "java"
}

archivesBaseName = "ZeusMod"
version = "2.0.0"

repositories {
    mavenCentral()
    maven {
        url = "https://hub.spigotmc.org/nexus/content/repositories/snapshots/"
    }
}

dependencies {
    compileOnly "org.spigotmc:spigot-api:$spigot_version"
}
